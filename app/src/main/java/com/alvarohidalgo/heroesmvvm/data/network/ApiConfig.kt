package com.alvarohidalgo.heroesmvvm.data.network

object ApiConfig {
    const val API_URL = ""

    const val API_KEY_PARAM = "api_key"

    //I could hide the ApiKey in a separate file and not add it to git, but lets be pragmatic
    const val API_TOKEN = ""
}